import { LoginForm } from "../components/LoginForm";
import { AuthLayout } from "@/components/Layout/";

export const Login = () => {
  return (
    <AuthLayout>
      <LoginForm />
    </AuthLayout>
  );
};
